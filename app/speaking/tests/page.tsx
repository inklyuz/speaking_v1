'use client'
import Link from 'next/link'
import { useEffect, useState } from 'react'
import { ArrowRight, Loader2, Mic2 } from 'lucide-react'
import { listPracticeTests, type SpeakingTestListItem } from '@/lib/api/speaking'
export default function SpeakingTestsPage(){const[tests,setTests]=useState<SpeakingTestListItem[]>([]);const[loading,setLoading]=useState(true);const[error,setError]=useState('');useEffect(()=>{listPracticeTests().then(setTests).catch(e=>setError(e instanceof Error?e.message:'Testlarni yuklab bo‘lmadi.')).finally(()=>setLoading(false))},[]);return <main className="min-h-screen bg-muted/30"><header className="border-b bg-background"><div className="container-shell flex h-16 items-center justify-between"><Link href="/speaking" className="font-bold tracking-tight">Mock<span className="text-primary">.Center</span></Link><Link href="/speaking/mock" className="text-sm font-semibold text-muted-foreground hover:text-foreground">Mock Speaking →</Link></div></header><section className="container-shell py-12"><p className="text-xs font-bold uppercase tracking-[0.18em] text-primary">Speaking practice</p><div className="mt-2 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><h1 className="text-3xl font-bold tracking-tight sm:text-4xl">Speaking testlar</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-muted-foreground">Kerakli variantni tanlang, mikrofonni tekshiring va testni real formatda topshiring.</p></div><div className="rounded-xl border bg-background px-4 py-3 text-sm"><Mic2 className="mr-2 inline size-4 text-primary" />{tests.length} ta faol test</div></div>{loading&&<div className="mt-10 flex items-center gap-2 text-sm text-muted-foreground"><Loader2 className="size-4 animate-spin"/>Testlar yuklanmoqda…</div>}{error&&<div className="mt-8 rounded-xl border border-red-200 bg-red-50 p-4 text-sm text-red-700">{error}</div>}{!loading&&!error&&<div className="mt-8 grid gap-4 md:grid-cols-2 lg:grid-cols-3">{tests.map((test,index)=><Link key={test.id} href={`/speaking/tests/${test.id}`} className="group rounded-2xl border bg-background p-6 transition hover:-translate-y-0.5 hover:border-primary/40 hover:shadow-md"><div className="flex items-start justify-between gap-4"><span className="flex size-10 items-center justify-center rounded-xl bg-primary/10 text-sm font-bold text-primary">{String(index+1).padStart(2,'0')}</span><span className="font-mono text-[10px] text-muted-foreground">{test.id}</span></div><h2 className="mt-6 font-bold">{test.title}</h2><p className="mt-2 text-sm text-muted-foreground">3 qism · kompyuterda · audio yozuv</p><div className="mt-6 flex items-center justify-between text-sm font-semibold"><span>Testni ko‘rish</span><ArrowRight className="size-4 transition group-hover:translate-x-1"/></div></Link>)}{tests.length===0&&<div className="col-span-full rounded-2xl border bg-background p-10 text-center text-sm text-muted-foreground">Hozircha faol Speaking testi yo‘q.</div>}</div>}</section></main>}
